{
  "name": "mylaps-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that proxies the MYLAPS Speedhive API for the activity viewer.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test/handler.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
